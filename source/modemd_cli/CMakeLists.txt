CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(modemd_cli)

SET(PROJECT_SOURCES
main.c
)

ADD_EXECUTABLE(modemd_cli ${PROJECT_SOURCES})

TARGET_LINK_LIBRARIES(modemd_cli modem)

INSTALL(TARGETS modemd_cli DESTINATION bin)
